#!/bin/sh
# Managed by puppet

# A non-login su session does not read profile.d, so PAM invokes the shared login notification explicitly.
if [ "$PAM_SERVICE" = "su" ]; then
    SH=$(command -v sh 2>/dev/null)
    if [ -n "$SH" ]; then
        $SH /etc/profile.d/99-login-notify.sh
    fi
fi
