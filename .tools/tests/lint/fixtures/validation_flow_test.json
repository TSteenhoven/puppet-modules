{
  "test_trailing_code_is_detected_at_every_enclosing_statement_block": {
    "patterns6": [
      "if $valid { notify { 'synthetic': } } else { fail('Invalid') }; $later = true",
      "if $outer { if $valid { notify { 'synthetic': } } else { fail('Invalid') }; notice('Later') }",
      "if $outer { if $valid { notify { 'synthetic': } } else { fail('Invalid') } }; notify { 'later': }",
      "if $first { if $second { if $valid { notice('Valid') } else { fail('Invalid') } } }; include later",
      "$value = if $valid { 'valid' } else { fail('Invalid') }; notice($value)",
      "[1].each |$value| { if $value > 0 { notice('Valid') } else { fail('Invalid') } }; notice('Later')",
      "[1].each |$value| { if $value > 0 { notice('Valid') } else { fail('Invalid') }; notice('Later') }"
    ]
  },
  "test_sequential_validations_must_be_nested": {
    "body": "if $first { notice('First') } else { fail('First missing') }; if $second { notice('Second') } else { fail('Second missing') }"
  },
  "test_multiple_final_diagnostics_and_message_preparation_keep_the_fallback_last": {
    "sample": "} elsif $alternative {\n  notice('Alternative')\n} else {",
    "body": "if $valid {\n  notice('Valid')\n} else {\n  $keys = join($invalid_keys, ', ')\n  $message = \"Invalid keys: ${keys}\"\n  warning($message)\n  fail('Cannot proceed')\n}\n"
  },
  "test_case_defaults_and_elsif_arms_follow_the_same_terminal_structure": {
    "sample": "case $state { 'active': { notice('Active') } default: { fail('Invalid') } }; notice('Later')",
    "sample2": "if $first { notice('First') } elsif $second { notice('Second') } else { fail('Neither') }",
    "sample3": "if $first { notice('First') } elsif $invalid { fail('Invalid') } else { fail('Neither') }"
  },
  "test_outer_alternative_with_implementation_may_follow_a_nested_validation": {
    "sample": "if $active { if $valid { notify { 'synthetic': } } else { fail('Invalid') } } else { notice('Inactive') }",
    "sample2": "case $state { 'active': { if $valid { notify { 'synthetic': } } else { fail('Invalid') } } default: { notice('Inactive') } }"
  },
  "test_declaration_boundaries_defaults_and_non_diagnostic_text_do_not_trigger_the_rule": {
    "sample": "if $valid { notice('Valid') } else { fail('Invalid') }\n}\nclass another {\nnotice('Other scope')",
    "body": "# warning('Only a comment')\n$text = 'fail(\"Only text\")'\n$content = @(CONTENT)\nwarning('Only heredoc content')\nCONTENT\nexample::warning('A different function')\nexample::fail('A different function')\n",
    "sample2": "notice('Regular class code')\n}\nfunction example::helper() { fail('Deferred function body')\n}\nclass another {\nnotice('Other scope')"
  },
  "test_nested_validation_else_arms_are_alternatives_not_trailing_implementation": {
    "body": "$ready = true\nif $ready {\n  if $valid {\n    notify { 'synthetic': }\n  } else {\n    fail('Invalid settings')\n  }\n} else {\n  fail('Missing prerequisite')\n}\n"
  }
}
