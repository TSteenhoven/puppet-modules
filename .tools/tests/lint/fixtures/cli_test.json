{
  "test_project_cli_options_select_checks_show_ignored_and_preserve_json_diagnostics": {
    "code": "$values = [1] + [2]\n$label = \"synthetic\"\n"
  },
  "test_documentation_fix_works_with_standard_checks_and_preserves_code_fixes": {
    "sample": "# @example Include the class\n#   include example\n",
    "code": "# @summary Manages the example.\n#\n# lint:ignore:140chars\n# A description with a documented default. A description with a documented default. A description with a documented default. A description with a documented default. A description with a documented default.\n# lint:endignore\n#\n# @example Include the class\n#   include example\n#\n# @api public\nclass example {\n  # Select the example value.\n  $value = \"synthetic\"\n}\n"
  },
  "test_resource_references_fix_works_with_standard_checks_and_is_idempotent": {
    "sample": "Notify['target'] -> [Package[\"zulu\"], Service['nginx'], Package[\"alpha\"], Service['apache2']]\n",
    "expected": "Notify['target'] -> [Package['alpha', 'zulu'], Service['apache2', 'nginx']]\n"
  },
  "test_single_reference_array_is_removed_with_standard_fixes": {
    "code": "# Declare the example dependency.\nnotify { 'example':\n  require => [\n      Package[\"a\",\"b\"],\n  ],\n}\n",
    "expected": "# Declare the example dependency.\nnotify { 'example':\n  require => Package['a', 'b'],\n}\n"
  },
  "test_parameter_alignment_uses_explicit_native_cli_fixing": {
    "code": "class example (\n  String $a= 'value',\n  Optional[String] $label=undef,\n) {}\n",
    "expected": "class example (\n  String           $a     = 'value',\n  Optional[String] $label = undef,\n) {}\n"
  },
  "test_if_sections_fail_without_inventing_or_moving_comments_with_fix": {
    "codes2": [
      "if $active { notice('Active') }\n",
      "$enabled = true\n$active = $enabled\n\n# Explain the active operation.\nif $active { notice('Active') }\n"
    ]
  },
  "test_block_variable_sections_offer_a_review_hint_without_moving_assignments_with_fix": {
    "corrected": "  $config_shell = stdlib::shell_escape($config)\n\n",
    "code": "# Prepare arguments only for the active check.\nif $active {\n  $config_shell = stdlib::shell_escape($config)\n\n  # Escape the check limits.\n  $timeout_shell = stdlib::shell_escape(String($timeout))\n}\n",
    "corrected2": "  # Escape the configuration path and check limits.\n  $config_shell = stdlib::shell_escape($config)"
  },
  "test_validation_structure_checks_enclosing_blocks_without_rewriting_them": {
    "corrected": "      notice('Valid')\n      notify { 'inside-validation': }",
    "code": "class example {\n  if $parent {\n    if $valid {\n      notice('Valid')\n    } else {\n      warning('Invalid settings')\n    }\n  } else {\n    fail('Missing parent')\n  }\n  notify { 'outside-validation': }\n}\n"
  },
  "test_class_check_reuse_does_not_change_evaluation_order_with_fix": {
    "declarations2": [
      "$enabled = defined(Class['synthetic']); notice($enabled)",
      "notice(defined(Class['synthetic'])); include synthetic; notice(defined(Class['synthetic']))"
    ]
  },
  "test_invalid_puppet_is_an_error_without_a_custom_execution_layer": {
    "code": "class example (String $value = ) { $other = \"value\" }\n"
  },
  "test_diagnostic_counts_are_independent_of_github_annotations": {
    "code": "$values = [\n      'first',\n      'second',\n]\n$other = [1] + [2]\n"
  },
  "test_resource_references_fix_preserves_comments_and_keeps_the_warning": {
    "code": "$refs = [\n  File['/tmp/z'], # Keep the reason attached to this resource.\n  Service['nginx'],\n  File['/tmp/a'],\n]\n"
  },
  "test_section_checks_fail_without_inventing_comments_with_fix": {
    "code": "$enabled = true\n# Keep the synthetic notification conditional.\nif $enabled {\n  notify { 'first': }\n}\nnotify { 'second': }\n"
  },
  "test_opening_brace_spacing_fails_even_after_a_line_length_suppression": {
    "code": "# Check the outer prerequisite.\nif $active { # lint:ignore:140chars\n\n  # Check the nested prerequisite.\n  if $ready {\n    notice('Ready')\n  }\n}\n"
  },
  "test_array_indentation_is_reported_and_fixed_explicitly": {
    "code": "$command = join([\n      'printf \"%s\"',\n      'synthetic',\n    ], ' ')\n"
  },
  "test_variable_sections_fail_without_inventing_an_explanation_with_fix": {
    "code": "# Prepare the label.\n$first = 'example'\n$second = $first\n$timeout = 30\n"
  },
  "test_branch_order_fails_without_rewriting_conditions_with_fix": {
    "code": "# Select the appropriate handling for the current state.\nif $active {\n  notice('Unavailable')\n} else {\n  # Prepare the fallback values used by the alternative path.\n  $first = 1\n  $second = 2\n}\n"
  },
  "test_monitoring_backend_check_does_not_rewrite_backend_conditions_with_fix": {
    "code": "# Register the check when monitoring is active.\n$active = $basic_settings::monitoring::package == 'synthetic_backend'\nif $active {\n  basic_settings::monitoring_custom { 'synthetic': }\n}\n"
  },
  "test_class_check_consumers_are_resolved_in_the_configured_modulepath": {
    "sample": "class example {\n  $enabled = defined(Class['synthetic'])\n  notice($enabled, template('example/state.erb'))\n}\n"
  },
  "test_a_new_default_check_is_not_disabled_by_the_project_configuration": {
    "sample": "require 'puppet-lint'\nPuppetLint.new_check(:future_default_check) do\n  def check\n    notify(:warning, message: 'New default check is active', line: 1, column: 1)\n  end\nend\n"
  },
  "test_default_and_project_checks_are_present_and_enabled": {
    "required_checks": [
      "140chars",
      "documentation",
      "parameter_order",
      "selector_inside_resource",
      "single_quote_string_with_variables",
      "class_inherits_from_params_class",
      "project_arrays",
      "project_class_check_reuse",
      "project_comment_spacing",
      "project_documentation",
      "project_files",
      "project_if_sections",
      "project_interface_calls",
      "project_layout",
      "project_monitoring_backend",
      "project_packages",
      "project_parameter_alignment",
      "project_parameter_order",
      "project_positive_flow",
      "project_puppet_urls",
      "project_resource_sections",
      "project_shell",
      "project_suppressions",
      "project_templates",
      "project_variable_sections"
    ]
  },
  "test_documentation_fix_requires_a_rescan_for_original_standard_length_findings": {
    "code": "# @summary Manages the example.\n#\n# A description with a default. A description with a default. A description with a default. A description with a default. A description with a default. A description with a default.\n#\n# @example Include the class\n#   include example\n#\n# @api public\nclass example {}\n"
  }
}
