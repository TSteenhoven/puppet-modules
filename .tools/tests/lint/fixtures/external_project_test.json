{
  "test_readme_entry_point_checks_only_own_files_and_ignores_personal_configuration": {
    "sample": "raise 'The consumer bundle must not be loaded'\n"
  },
  "test_external_interfaces_follow_module_order_and_keep_dependencies_outside_style_scope": {
    "sample": "class { 'profile': }\nclass { 'shared': }\n",
    "sample2": "class { 'profile': }\nprofile::item { 'synthetic': }\n"
  },
  "_setup": {
    "profile": "# @summary Provides a synthetic local interface.\n#\n# @param value A synthetic input.\n#\n# @example Declare the local profile\n#   class { 'profile': value => 'synthetic' }\n#\n# @api public\nclass profile (\n  String $value,\n) {\n}\n",
    "site": "$values = concat([1], [2])\nclass { 'profile':\n  value => 'synthetic',\n}\n\n# Exercise the interface supplied by the shared module path as well.\nclass { 'shared':\n  value => 'synthetic',\n}\n"
  },
  "test_consumer_configuration_is_required_and_shared_changes_take_effect": {
    "log_format": "--log-format=shared:%{check}:%{line}: %{message}\n"
  }
}
